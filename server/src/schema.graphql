
type Query {
    test : User,
    getUsers : [User]
}

type Mutation{
    addUser(email:String!,password:String!):User
    userAuthenticate(email:String!,password:String!): User

}

type Subscription {
    newUserCreated : User
}